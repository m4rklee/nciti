{
  "name": "baoming-ti-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/scoring.test.js",
    "start": "python3 -m http.server 8080",
    "build": "rm -rf build && mkdir -p build && cp -r index.html css js images package.json README.md build/ && echo 'Static site built to ./build'"
  }
}
