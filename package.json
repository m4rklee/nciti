{
  "name": "baoming-ti-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/",
    "start": "python3 -m http.server 8080",
    "export:content": "node scripts/export-content.mjs",
    "build": "rm -rf build && mkdir -p build && cp -r index.html admin.html css js images data package.json README.md build/ && echo 'Static site built to ./build'"
  }
}
